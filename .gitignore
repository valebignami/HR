# --- Non pubblicare su GitHub Pages (cartella pubblica) ---

# SQL: schema DB, policy RLS, definizioni RPC — non vanno esposti pubblicamente
sql/

# Documentazione interna (contesto aziendale, architettura)
*.md

# Eventuali segreti / ambiente (per sicurezza, non dovrebbero mai esistere qui)
.env
.env.*
*credentials*.json
*secret*
token.json

# File di sistema
desktop.ini
Thumbs.db
.DS_Store

# Debris di sessioni Playwright MCP (repo pubblicamente servito)
.playwright-mcp/

# Strumenti locali dell'agente (launch.json della prova nel browser)
.claude/

# Backup scaricati dall'app: vanno nell'archivio, mai nel repo pubblico
Backup app/

# Rapporti di fase (gia' coperti da *.md, ma l'intenzione va scritta)
RAPPORTO_*
